version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    volumes:
      - ./frontend:/app         # Mount local code into the container
      - /app/node_modules       # Prevent overriding node_modules
    ports:
      - "5173:5173"             # Vite dev server port
    command: npm run dev        # Start dev server
    environment:
      - NODE_ENV=development

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - ./backend:/app
      - /app/node_modules
    ports:
      - "5000:5000"
    command: npm run dev
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true  # Ensures file changes are detected in Docker

